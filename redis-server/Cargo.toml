[package]
name = "redis_server"
version = "0.1.0"
edition = "2024"

[dependencies]
commands = { path = "../commands" }
crc16 = "0.4"
log = { path = "../log" }
resp = { path = "../resp" }
uuid = { version = "1.16.0", features = ["v4"] }
